스트림
배열이나 컬렉션 같은 데이터 연속으로 하나씩 참조하여 가공처리 할 수 있게 도와주는 클래스
불필요한 반복문이나 분기문등을 생략 할 수있어 코드를 줄일수 있다.

- 데이터변경은 없다. 읽어서 처리 할 뿐 원변 변경은 없다.
- 일회용
- 코드상으로 반복문 노출X

Arrays.stream이나 Collection.stream() 사용

stream은 각종 collection의 데이터를 다룰수 있는 메소드를 제공
filter(Predicate)